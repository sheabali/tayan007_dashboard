import JobsPage from "@/components/module/Dashboard/Jobs";

export const metadata = {
  title: "Jobs Management | Admin Dashboard",
  description: "Manage and track jobs in production, review stage, and completed.",
};

const Jobs = () => {
    return <JobsPage />;
};

export default Jobs;
